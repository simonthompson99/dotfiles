# selecting/filtering rows
,       select all rows with current value in current column
"       new sheet with only selected rows
s/t     select/toggle rows
gu      unselect all rows
z| expr	filter rows based on a python expression

# loading data
vd -f tsv file.csv	force a particular format to load data
